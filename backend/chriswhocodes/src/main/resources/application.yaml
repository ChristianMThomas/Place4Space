spring:
  application:
    name: chriswhocodes
  datasource:
    url: jdbc:postgresql://localhost:5432/place4space
    username: postgres
    password: 0634
  jpa:
    hibernate:
      ddl-auto: update
